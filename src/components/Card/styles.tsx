import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    minHeight: 60,
    marginTop: 20,
    padding: 16,
    borderRadius: 6,
    borderWidth: 0.3,
    borderColor: "black",
    justifyContent: "center",
    alignItems: "center",
  },
});
